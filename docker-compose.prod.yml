# Optional production overrides (same port 27296).
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# Site: http://<host>:27296

services:
  caddy:
    restart: always
  backend:
    restart: always
  frontend:
    restart: always
  postgres:
    restart: always
  redis:
    restart: always
